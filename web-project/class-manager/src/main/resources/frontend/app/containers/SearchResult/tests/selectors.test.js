// import { selectSearchResultDomain } from '../selectors';

describe('selectSearchResultDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
