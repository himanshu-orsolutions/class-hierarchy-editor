// import { selectCustomTreeItemDomain } from '../selectors';

describe('selectCustomTreeItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
